# AGENTS.md

이 문서는 이 저장소에서 코딩 에이전트가 따라야 할 최소 작업 기준을 정의합니다.

## 1. 프로젝트 개요 및 일반 작업 지침

- 현재 저장소의 변경은 **요청 범위 내**에서만 수행합니다.
- 기존 코드베이스의 규칙(구조, 네이밍, 스타일, 패턴)을 우선 따릅니다.
- 불필요한 추상화, 과도한 리팩터링, 무관한 최적화를 추가하지 않습니다.
- 문제를 임시로 우회하기보다 원인에 맞는 수정으로 해결합니다.
- 변경 전후 영향을 확인하고, 관련 영역만 정밀하게 수정합니다.

## 2. Python / TypeScript 코딩 가이드라인

### 공통
- 타입 안정성을 우선합니다. `any`/무분별한 캐스팅/타입 무시는 지양합니다.
- 오류를 숨기지 않고 명시적으로 처리하며, 실패 원인이 드러나도록 합니다.
- 중복 코드를 늘리지 말고 기존 유틸리티/헬퍼를 우선 재사용합니다.
- 새 의존성은 꼭 필요한 경우에만 추가하고, 이유와 영향을 명확히 합니다.

### Python
- 함수 시그니처와 핵심 데이터 구조에 타입 힌트를 사용합니다.
- 광범위한 `except Exception` 남용을 피하고, 가능한 한 구체 예외를 처리합니다.
- I/O, 네트워크, 파일 처리 등 실패 가능 구간은 예외/에러 경로를 명확히 작성합니다.

### TypeScript
- `strict` 환경을 전제로 타입 오류를 우회하지 않습니다.
- nullable/optional 값은 가드로 좁힌 뒤 사용합니다.
- 런타임 입력(외부 API, 사용자 입력)은 신뢰하지 말고 검증 후 사용합니다.

## 3. 테스트 및 유효성 검사 원칙

- 변경과 직접 관련된 최소 범위의 검증(테스트/포맷팅/린트/타입 검사/빌드)을 우선 수행합니다.
- 저장소에 **이미 구성된 도구와 명령만** 사용합니다.
- 아직 존재하지 않는 툴체인, 스크립트, 실행 명령을 임의로 추가/가정하지 않습니다.
- 문서 전용 변경은 별도 검증이 필요하지 않으며, 저장소 정책이 있으면 그 정책을 따릅니다.

## 4. 보안

- 비밀정보(토큰, 키, 자격증명, 개인정보)를 코드/로그/문서에 남기지 않습니다.
- `.env`, `.env.*`(예: `.env.local`), `*.pem`, `*.key`, `credentials.*`, `secrets.*` 등 비밀정보가 포함될 수 있는 파일은 커밋하지 않습니다. 실제 값이 없는 `.env.example` 같은 템플릿만 허용합니다.
- 입력 데이터는 신뢰하지 않고 검증 및 이스케이프를 적용합니다.
- 권한, 인증, 민감 데이터 접근 로직 변경 시 보안 영향을 우선 점검합니다.

## 5. 문서화

- 동작/인터페이스/운영 방법이 바뀌면 관련 문서를 함께 갱신합니다.
- 사용자/운영자가 이해해야 할 변경 사항은 간결하고 재현 가능하게 기록합니다.
- 코드 주석은 필요한 맥락에만 최소한으로 작성합니다.

## 6. 주의사항 및 가드레일

- 요청하지 않은 대규모 구조 변경, 파일 이동, 포맷 전면 변경은 하지 않습니다.
- 기존 동작을 바꾸는 경우 의도를 명확히 하고 영향 범위를 제한합니다.
- 실패를 숨기는 폴백, 무조건 성공처럼 보이게 하는 처리, 광범위한 예외 무시는 금지합니다.
- 파괴적 명령(강제 리셋/강제 삭제)은 명시적 요청 없이 수행하지 않습니다.

## 7. Git 커밋 및 Pull Request 규칙

- 커밋은 하나의 논리적 변경 단위로 작고 명확하게 구성합니다.
- 커밋 메시지는 변경 의도와 결과가 드러나게 작성합니다.
- Pull Request에는 최소한 다음을 포함합니다:
  - 변경 배경/목적
  - 핵심 변경 내용
  - 검증 방법 또는 검증 결과(저장소에 구성된 범위 내)
  - 호환성/리스크/후속 작업(해당 시)
