# 급식 배틀 - 학교 급식 조회 앱

## 1. 문서 개요

### 1.1 목적

이 문서는 NEIS 공개 API의 학교 및 급식 데이터를 활용해 초·중·고등학교의 중식 메뉴를 조회하고, 같은 날짜의 두 학교 중식을 비교하는 웹 애플리케이션의 제품 요구사항을 정의한다. 제품의 사용자 가치, 범위, 사용자 흐름, UI 원칙, 기능 및 품질 요구사항에 대한 단일 기준으로 사용한다.

### 1.2 제품 한 줄 설명

학교 이름 일부만 검색해 학교를 선택하고, 달력에서 날짜 범위를 지정해 날짜별 중식을 확인하거나 같은 날짜의 두 학교 중식을 나란히 비교하는 웹 애플리케이션이다.

### 1.3 개발 목적

- 분산되어 있거나 접근하기 어려운 학교 급식 정보를 간단한 3단계 흐름으로 제공한다.
- 학생에게는 친근하고 재미있는 경험을, 학부모와 교직원에게는 명확하고 신뢰할 수 있는 정보를 제공한다.
- 두 학교의 같은 날짜 중식을 한눈에 비교해 “급식 배틀”이라는 제품 정체성을 제공한다.
- 향후 평가 및 분석 기능으로 확장 가능한 제품 기반을 마련한다.

## 2. 대상 사용자

### 2.1 주요 사용자

- 자신의 학교 급식 메뉴를 확인하려는 초·중·고등학생
- 자녀의 학교 급식을 확인하려는 학부모 및 보호자

### 2.2 보조 사용자

- 학교 급식 정보를 확인하거나 비교하려는 교직원
- 학교별 급식 데이터에 관심이 있는 일반 사용자

## 3. 제품 범위

### 3.1 MVP 범위

- 두 글자 이상의 학교 이름 일부를 입력한 학교 검색
- 검색 결과에서 학교 선택
- 달력형 날짜 범위 선택기(Date Range Picker)를 이용한 시작일 및 종료일 선택
- 선택한 학교와 날짜 범위에 해당하는 중식 정보 조회
- 날짜별 메뉴와 NEIS가 제공하는 부가 정보 표시
- 급식 조회와 독립된 급식 배틀 화면
- 배틀 화면에서 비교할 두 학교와 한 날짜 선택
- 같은 날짜의 두 학교 중식 핵심 정보를 나란히 비교
- 상세 영양정보와 원산지 정보를 “자세히 보기”에서 선택적으로 표시
- 검색, 입력, 데이터 및 시스템 오류 상태 안내
- 데스크톱과 모바일 웹 지원

### 3.2 현재 범위에서 제외

- 회원가입, 로그인 및 사용자 프로필
- 학교 즐겨찾기 및 검색 기록 저장
- 급식 평가, 투표, 댓글 및 공유
- 학교별 점수, 승자 판정 또는 사용자 투표
- AI를 이용한 급식 평가 및 분석
- 조식 및 석식 조회
- 급식 데이터의 생성, 수정 또는 삭제
- 푸시 알림 및 개인화 추천

> MVP의 “배틀”은 두 학교의 객관적인 급식 정보를 나란히 보여주는 비교 경험이다. 앱이 점수를 매기거나 승자를 판정하는 기능은 후속 단계에서 다룬다.

## 4. 핵심 사용자 흐름

### 4.1 급식 조회

사용자는 다음 3단계를 순서대로 진행한다.

1. **학교 검색 및 선택**
   - 사용자가 학교 이름의 일부를 입력한다.
   - 일치하는 학교 목록에서 원하는 학교를 선택한다.
2. **날짜 범위 선택**
   - 사용자가 달력형 날짜 범위 선택기에서 시작일과 종료일을 선택한다.
   - 기본 범위는 오늘을 포함한 최근 7일인 `오늘-6일 ~ 오늘`이다.
   - 사용자는 이번 달과 직전 달에 포함되는 날짜만 선택할 수 있다.
   - 선택한 범위를 확인하고 필요하면 다시 선택한다.
3. **급식 결과 확인**
   - 사용자가 조회 버튼을 누른다.
   - 선택 기간의 중식 정보를 날짜순으로 확인한다.

학교나 날짜를 변경하면 사용자는 같은 흐름에서 다시 조회할 수 있어야 한다.

### 4.2 급식 배틀

사용자는 급식 조회 화면을 거치지 않고 독립된 급식 배틀 화면에서 다음 3단계를 진행한다.

1. **두 학교 선택**
   - 사용자가 학교 이름 일부로 첫 번째 학교와 두 번째 학교를 각각 검색하고 선택한다.
2. **배틀 날짜 선택**
   - 사용자가 달력형 날짜 선택기(Date Picker)에서 비교할 날짜 하나를 선택한다.
   - 사용자는 이번 달과 직전 달에 포함되는 날짜만 선택할 수 있다.
3. **배틀 결과 확인**
   - 사용자가 배틀 시작 버튼을 누른다.
   - 두 학교의 학교명, 메뉴 및 열량을 한눈에 비교한다.
   - 더 많은 정보가 필요한 사용자는 각 학교의 “자세히 보기”를 펼쳐 영양정보와 원산지 정보를 확인한다.

배틀 결과에서 첫 번째 학교, 두 번째 학교 또는 날짜를 변경해 다시 비교할 수 있어야 한다. 급식 조회 화면의 학교 및 날짜 선택 상태와 급식 배틀 화면의 선택 상태는 서로 영향을 주지 않아야 한다.

## 5. UI 및 경험 원칙

### 5.1 화면 구조

- 전역 내비게이션에서 **급식 조회**와 **급식 배틀**을 서로 동등한 메뉴로 제공한다.
- 급식 조회와 급식 배틀은 별도의 화면으로 구성하며, 한 기능을 사용하기 위해 다른 기능을 먼저 거칠 필요가 없어야 한다.
- 현재 사용 중인 메뉴를 내비게이션에서 명확히 표시한다.
- 두 화면은 학교 검색 등 공통된 조작 방식을 사용하되 입력값과 결과 상태를 공유하지 않는다.
- 급식 조회 화면은 `학교 검색 → 날짜 범위 선택 → 결과 표시`의 3단계 스테퍼를 사용한다.
- 급식 배틀 화면은 `두 학교 선택 → 날짜 선택 → 비교 결과`의 3단계 스테퍼를 사용한다.

### 5.2 시각 스타일

UI는 **밝고 친근한 학교 스포츠 중계형 스타일**을 사용한다. 급식의 따뜻하고 일상적인 이미지와 “배틀”의 활기찬 인상을 결합하되, 정보 조회를 방해하는 과도한 게임 효과나 네온 표현은 사용하지 않는다.

- 크림색 또는 밝은 중성색을 기본 배경으로 사용한다.
- 주황색과 노란색을 주요 강조색으로 사용한다.
- 둥근 모서리와 급식판을 연상시키는 카드형 레이아웃을 적용한다.
- 음식 및 학교 관련 일러스트나 아이콘은 정보 이해를 돕는 범위에서 사용한다.
- 각 화면에서 현재 진행 단계를 큰 3단계 스테퍼로 명확히 보여준다.
- 결과는 날짜별 카드로 구분해 빠르게 훑어볼 수 있게 한다.
- 배틀 결과는 두 학교를 좌우에 배치하고 중앙에 `VS`를 표시해 비교 관계를 명확히 한다.
- 배틀 화면의 기본 상태에는 학교명, 메뉴 및 열량 등 핵심 정보만 표시한다.
- 영양정보와 원산지 같은 세부 정보는 기본적으로 접어 두고 “자세히 보기”를 통해 사용자가 원할 때만 펼친다.
- 재미있는 분위기를 유지하되, 학생뿐 아니라 학부모와 교직원도 신뢰할 수 있는 단정한 정보 구조를 사용한다.

### 5.3 반응형 디자인

- 모바일 화면을 우선하여 설계한다.
- 주요 버튼과 날짜 셀은 터치하기 충분한 크기로 제공한다.
- 작은 화면에서는 콘텐츠를 한 열로 배치하고 가로 스크롤을 최소화한다.
- 모바일 배틀 화면에서는 두 학교 카드를 세로로 배치하되, 학교명과 `VS` 표시로 비교 대상을 혼동하지 않게 한다.
- 데스크톱에서는 검색 조건과 결과의 가독성을 높이되 모바일과 동일한 흐름을 유지한다.

### 5.4 접근성

- 본문은 기본 16px 이상의 읽기 쉬운 크기를 사용한다.
- 텍스트와 배경은 충분한 명도 대비를 확보한다.
- 상태와 선택 여부를 색상에만 의존하지 않고 텍스트, 아이콘 또는 형태로 함께 표현한다.
- 모든 입력 요소와 날짜 선택 기능은 키보드로 조작할 수 있어야 한다.
- 입력 레이블, 오류 메시지 및 버튼 이름은 보조 기술이 이해할 수 있도록 제공한다.

## 6. 기능 요구사항

### FR-1. 학교 검색

- 사용자는 학교 이름의 일부를 검색어로 입력할 수 있어야 한다.
- 앱은 입력한 검색어와 일치하는 학교 목록을 표시해야 한다.
- 각 결과는 같은 이름의 학교를 구분할 수 있도록 최소한 학교명, 학교급 및 지역 정보를 제공해야 한다.
- 검색 진행 중임을 사용자가 알 수 있어야 한다.
- 앞뒤 공백을 제거한 학교 검색어가 두 글자 이상일 때만 검색을 실행해야 한다.
- 검색어가 두 글자 미만이면 API를 호출하지 않고 두 글자 이상 입력하도록 안내해야 한다.

### FR-2. 학교 선택

- 사용자는 검색 결과에서 한 개의 학교를 선택할 수 있어야 한다.
- 선택한 학교는 시각적으로 명확하게 표시해야 한다.
- 사용자는 날짜 선택 또는 조회 전에 선택한 학교를 변경할 수 있어야 한다.
- 학교를 선택하지 않은 상태에서는 급식 조회를 실행할 수 없어야 한다.

### FR-3. 날짜 범위 선택

- 날짜 입력에는 달력형 **날짜 범위 선택기(Date Range Picker)** 컴포넌트를 사용해야 한다.
- 화면 진입 시 기본 선택 범위는 오늘을 포함한 최근 7일인 `오늘-6일 ~ 오늘`이어야 한다.
- 선택 가능한 기간은 이번 달 1일부터 말일까지와 직전 달 1일부터 말일까지로 제한해야 한다.
- 연도가 바뀌는 1월에는 직전 달을 전년도 12월로 계산해야 한다.
- 사용자는 달력에서 시작일과 종료일을 직접 선택할 수 있어야 한다.
- 시작일을 선택한 후 종료일까지의 범위를 달력 위에서 시각적으로 구분해야 한다.
- 선택된 시작일과 종료일을 사용자가 명확히 확인할 수 있어야 한다.
- 사용자는 선택한 범위를 초기화하거나 다시 선택할 수 있어야 한다.
- 종료일은 시작일보다 빠를 수 없다.
- 잘못된 범위는 조회 전에 차단하고 수정 방법을 안내해야 한다.
- 선택 가능 기간 밖의 날짜는 비활성화하고 키보드나 직접 입력으로도 선택할 수 없어야 한다.
- 모바일에서도 날짜 선택 과정이 화면 밖으로 잘리거나 조작하기 어렵지 않아야 한다.

### FR-4. 급식 조회

- 사용자는 학교와 유효한 날짜 범위를 선택한 후 중식 정보를 조회할 수 있어야 한다.
- 조회 조건에는 선택된 학교를 식별하는 정보와 시작일 및 종료일이 사용되어야 한다.
- 조회 중에는 중복 요청을 방지하고 로딩 상태를 표시해야 한다.
- 새로운 조회를 실행하면 결과는 최신 검색 조건에 맞게 갱신되어야 한다.

### FR-5. 날짜별 급식 결과

- 결과 상단에 선택한 학교명과 조회 기간을 표시해야 한다.
- 결과는 날짜 오름차순으로 표시해야 한다.
- 각 날짜 카드는 최소한 급식일과 메뉴를 표시해야 한다.
- NEIS 데이터가 제공되는 경우 열량, 영양정보 및 원산지 정보를 함께 표시할 수 있어야 한다.
- 알레르기 유발 식품 번호 등 메뉴에 포함된 원문 표기가 있다면 의미를 잃지 않도록 제공해야 한다.
- 메뉴의 줄바꿈 또는 구분 정보를 사용자가 읽기 쉬운 형태로 표현해야 한다.
- 사용자는 검색 조건으로 돌아가 학교 또는 날짜를 변경할 수 있어야 한다.

### FR-6. 배틀 학교 선택

- 사용자는 독립된 급식 배틀 화면에서 첫 번째 학교와 두 번째 학교를 각각 검색하고 선택할 수 있어야 한다.
- 두 학교 모두 학교 이름 일부로 검색하고 결과 목록에서 한 곳을 선택할 수 있어야 한다.
- 같은 학교를 양쪽에 선택할 수 없어야 하며 그 이유를 안내해야 한다.
- 사용자는 배틀 실행 전에 두 학교 중 하나를 변경하거나 선택을 취소할 수 있어야 한다.

### FR-7. 배틀 날짜 선택

- 배틀 날짜 입력에는 달력형 **날짜 선택기(Date Picker)** 컴포넌트를 사용해야 한다.
- 선택 가능한 날짜는 이번 달과 직전 달에 포함된 날짜로 제한해야 한다.
- 사용자는 두 학교를 비교할 날짜 하나를 달력에서 선택할 수 있어야 한다.
- 선택된 날짜를 명확히 표시하고 초기화하거나 다시 선택할 수 있어야 한다.
- 날짜를 선택하지 않은 상태에서는 배틀을 실행할 수 없어야 한다.
- 선택 가능 기간 밖의 날짜는 비활성화하고 키보드나 직접 입력으로도 선택할 수 없어야 한다.
- 급식 조회 화면에서 선택한 날짜 범위가 배틀 날짜에 자동으로 적용되지 않아야 한다.

### FR-8. 같은 날짜 중식 비교

- 앱은 두 학교의 같은 날짜 중식만 비교해야 한다.
- 사용자는 서로 다른 두 학교와 날짜 하나를 선택한 후 배틀을 실행할 수 있어야 한다.
- 비교 화면에는 선택한 날짜와 두 학교명을 명확히 표시해야 한다.
- 데스크톱에서는 두 학교 카드를 좌우에 배치하고 중앙에 `VS`를 표시해야 한다.
- 각 학교 카드의 기본 상태에는 메뉴와 열량을 표시해야 한다.
- 앱은 기본 비교만으로 특정 학교의 우열, 점수 또는 승자를 표시하지 않아야 한다.
- 각 학교 카드에는 독립적으로 조작할 수 있는 “자세히 보기”를 제공해야 한다.
- “자세히 보기”는 기본적으로 닫힌 상태여야 하며, 펼치면 NEIS가 제공하는 영양정보와 원산지 정보를 표시해야 한다.
- 상세 정보가 제공되지 않은 항목은 추정하지 않고 “제공된 정보가 없습니다”라고 표시해야 한다.
- 사용자는 첫 번째 학교, 두 번째 학교 또는 날짜를 변경해 다시 비교할 수 있어야 한다.

## 7. 상태 및 예외 처리

| 상황 | 사용자 경험 요구사항 |
|---|---|
| 초기 화면 | 현재 선택한 급식 조회 또는 급식 배틀의 3단계 흐름과 시작 방법을 안내한다. |
| 학교 검색 중 | 검색 중임을 명확히 표시한다. |
| 학교 검색어 두 글자 미만 | 두 글자 이상 입력하도록 안내하고 검색 요청을 보내지 않는다. |
| 학교 검색 결과 없음 | “검색 결과가 없습니다”라는 메시지와 학교명 확인 또는 다른 검색어 사용 방법을 안내한다. |
| 학교 검색 실패 | 일시적인 오류임을 알리고 재시도 수단을 제공한다. |
| 학교 미선택 | 학교 선택이 필요함을 해당 단계 가까이에 표시한다. |
| 날짜 미선택 | 시작일과 종료일 선택이 필요함을 안내한다. |
| 잘못된 날짜 범위 | 종료일이 시작일보다 빠를 수 없음을 설명하고 조회를 차단한다. |
| 선택 가능 기간 밖의 날짜 | 이번 달과 직전 달만 선택할 수 있음을 안내하고 조회 또는 배틀을 차단한다. |
| 급식 조회 중 | 결과 영역에 로딩 상태를 표시하고 중복 조회를 막는다. |
| 선택 기간 전체에 급식 정보 없음 | “선택한 기간에 중식 정보가 없습니다”라는 메시지와 다른 날짜 선택 방법을 안내한다. |
| 일부 날짜에 급식 정보 없음 | 데이터가 있는 날짜는 표시하고, 없는 날짜는 급식 정보가 없음을 날짜별로 구분한다. |
| 급식 조회 실패 | 기존 결과를 성공처럼 표시하지 않고 오류 메시지와 재시도 수단을 제공한다. |
| 배틀 날짜 미선택 | 비교할 날짜 하나를 선택해야 함을 안내하고 배틀 실행을 차단한다. |
| 배틀에 같은 학교 선택 | 서로 다른 학교를 선택해야 함을 안내하고 배틀 실행을 차단한다. |
| 배틀 학교 검색 결과 없음 | 검색어를 확인하거나 다른 학교명을 입력하도록 안내한다. |
| 한 학교의 해당 날짜 중식 없음 | 비교할 수 없음을 해당 학교 카드에 표시하고 다른 학교 또는 날짜 선택 방법을 안내한다. |
| 배틀 조회 실패 | 비교 결과를 성공처럼 표시하지 않고 오류 메시지와 재시도 수단을 제공한다. |
| 네트워크 연결 실패 | 연결 상태를 확인하도록 안내하고 재시도할 수 있게 한다. |

오류 메시지는 원인을 숨기지 않되, 내부 시스템 정보나 민감한 정보를 노출하지 않는 사용자 친화적인 문장으로 표시한다.

## 8. 비기능 요구사항

### 8.1 사용성

- 처음 방문한 사용자도 별도의 도움말 없이 핵심 조회 흐름을 완료할 수 있어야 한다.
- 각 단계의 필수 입력과 다음 행동이 명확해야 한다.
- 빈 화면 대신 현재 상태와 사용자가 취할 다음 행동을 안내해야 한다.
- 배틀 화면은 핵심 비교 정보를 먼저 보여주고 상세 정보는 사용자가 요청할 때만 노출해야 한다.

### 8.2 성능

- 검색 및 조회 요청이 진행 중임을 즉시 표시해 사용자가 입력 실패로 오해하지 않도록 한다.
- 반복 클릭에 의한 동일 요청의 중복 실행을 방지한다.
- 긴 날짜 범위나 많은 결과에서도 날짜별 카드를 안정적으로 탐색할 수 있어야 한다.

### 8.3 호환성

- 최신 주요 데스크톱 및 모바일 브라우저에서 핵심 흐름이 동작해야 한다.
- 화면 크기나 입력 방식과 무관하게 동일한 정보를 제공해야 한다.

### 8.4 데이터 신뢰성 및 개인정보

- 급식 정보는 NEIS 공개 데이터에 기반하며, 앱이 임의로 메뉴나 영양 정보를 추정하지 않아야 한다.
- 데이터가 없거나 외부 서비스가 실패한 상태를 정상 결과처럼 표시하지 않아야 한다.
- MVP는 개인정보를 요청하거나 저장하지 않아야 한다.

## 9. 성공 지표

- 학교 검색을 시작한 사용자의 급식 조회 완료율
- 학교 선택부터 결과 표시까지의 핵심 흐름 이탈률
- 급식 배틀 화면에 진입한 사용자 중 두 학교와 날짜를 선택해 비교를 완료한 비율
- 배틀 화면에서 “자세히 보기”를 사용한 비율
- 검색 결과 없음, 잘못된 날짜 범위 및 조회 실패 후 재시도 성공률
- 모바일과 데스크톱에서의 핵심 흐름 완료율 차이
- 급식 조회 요청의 성공률

초기 버전에서는 지표의 기준값을 수집하고, 이후 개선 목표를 설정한다.

## 10. 인수 조건

- [ ] 앱 제목이 “급식 배틀 - 학교 급식 조회 앱”으로 표시된다.
- [ ] 대상 사용자와 개발 목적이 제품 경험에 반영된다.
- [ ] 전역 내비게이션에서 급식 조회와 급식 배틀을 각각 독립된 화면으로 이동할 수 있다.
- [ ] 급식 조회를 수행하지 않고도 급식 배틀을 바로 시작할 수 있다.
- [ ] 두 화면의 학교 및 날짜 선택 상태와 결과가 서로 영향을 주지 않는다.
- [ ] 사용자는 학교 이름 일부로 학교를 검색할 수 있다.
- [ ] 앞뒤 공백을 제거한 학교 검색어가 두 글자 이상일 때만 검색 요청이 실행된다.
- [ ] 학교 검색 결과에서 학교명, 학교급 및 지역을 확인하고 한 학교를 선택할 수 있다.
- [ ] 급식 조회 화면은 학교 검색, 날짜 범위 선택, 결과 표시의 3단계 스테퍼를 제공한다.
- [ ] 날짜 선택에 달력형 날짜 범위 선택기(Date Range Picker)가 사용된다.
- [ ] 급식 조회 화면의 기본 날짜 범위는 오늘을 포함한 최근 7일인 `오늘-6일 ~ 오늘`이다.
- [ ] 급식 조회와 급식 배틀 모두 이번 달과 직전 달의 날짜만 선택할 수 있다.
- [ ] 사용자는 달력에서 시작일과 종료일을 선택하고 선택 범위를 확인, 초기화 및 재선택할 수 있다.
- [ ] 종료일이 시작일보다 빠른 날짜 범위는 조회되지 않고 명확한 안내가 표시된다.
- [ ] 선택한 학교와 날짜 범위의 중식 결과가 날짜별로 표시된다.
- [ ] 결과에 급식일과 메뉴가 표시되고, 제공되는 경우 열량, 영양정보 및 원산지 정보도 확인할 수 있다.
- [ ] 급식 배틀 화면은 두 학교 선택, 날짜 선택, 비교 결과의 3단계 스테퍼를 제공한다.
- [ ] 급식 배틀 화면에서 두 학교를 각각 검색하고 선택할 수 있다.
- [ ] 배틀 날짜는 달력형 날짜 선택기(Date Picker)에서 한 날짜만 선택한다.
- [ ] 같은 학교를 양쪽 비교 대상으로 선택할 수 없다.
- [ ] 두 학교의 같은 날짜 중식이 학교명, 메뉴 및 열량 중심으로 나란히 표시된다.
- [ ] 데스크톱 배틀 화면은 두 학교 카드 사이에 `VS`를 표시한다.
- [ ] 각 학교의 영양정보와 원산지는 기본적으로 접혀 있으며 “자세히 보기”로 펼칠 수 있다.
- [ ] 비교 결과는 점수, 우열 또는 승자를 임의로 표시하지 않는다.
- [ ] 한 학교라도 해당 날짜 중식이 없으면 비교 불가 상태와 다음 행동을 안내한다.
- [ ] 학교 검색 결과 없음, 학교 미선택, 날짜 미선택, 잘못된 날짜 범위, 급식 정보 없음, 네트워크 및 조회 실패 상태가 각각 구분되어 안내된다.
- [ ] UI는 밝고 친근한 학교 스포츠 중계형 스타일과 급식판 형태의 카드 레이아웃을 사용한다.
- [ ] 핵심 흐름은 모바일과 데스크톱에서 모두 사용할 수 있다.
- [ ] 키보드 조작, 명도 대비, 입력 레이블 및 색상 외 상태 표현을 포함한 기본 접근성 요구사항을 충족한다.

## 11. 향후 확장 고려사항

- 평가 기준에 따른 점수, 승자 또는 동점 결과 표시
- AI 에이전트를 활용한 급식 분석 및 근거 설명
- 학교 즐겨찾기와 최근 검색
- 급식 알림 및 결과 공유

이 항목은 MVP 인수 조건에 포함하지 않으며, 별도의 요구사항 검토를 거쳐 진행한다.
